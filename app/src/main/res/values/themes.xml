<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RetailManagementSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/primary_green</item>

        <item name="colorPrimaryContainer">@color/light_green</item>

        <item name="colorOnPrimary">@color/white</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/dark_green</item>

        <!-- Background -->
        <item name="android:windowBackground">@color/background</item>
    </style>

    <style name="Theme.RetailManagementSystem" parent="Base.Theme.RetailManagementSystem" />
</resources>